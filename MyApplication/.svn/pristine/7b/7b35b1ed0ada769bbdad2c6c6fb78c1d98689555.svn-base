package zxing.activity;

import android.app.Activity;
import android.os.Bundle;
import android.widget.TextView;

import com.younle.younle624.myapplication.R;

/**
 * Created by Administrator on 2016/5/26.
 */
public class AboutActivity extends Activity {
    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.about_card_code);
        TextView mTvText= (TextView) findViewById(R.id.tv_text);
       /* mTvText.setOnClickListener(new View.OnClickListener() {
            @Override
            public void onClick(View v) {
                Intent intent = new Intent(AboutActivity.this, CaptureActivity.class);
                startActivity(intent);
            }
        });*/
    }
}
